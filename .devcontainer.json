{
	"name": "blog",
	"build": {
		"dockerfile": "Dockerfile"
	},
	"containerEnv": {"CPPFLAGS":"-DPNG_ARM_NEON_OPT=0"},
	"customizations": {
		"vscode": {
			"extensions": []
		}
	},
	"onCreateCommand": "git config user.name $(git --no-pager log --pretty=\"%an\" | head -n 1) && git config user.email $(git --no-pager log --pretty=\"%ce\" | head -n 1) && npm i"
}